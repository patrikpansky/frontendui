export const RoleTypeSelect = ({}) => {
    return (
        <select>
            <option>RoleType</option>
        </select>
    )
}

export const RoleTypeOptions = ({}) => {
    
    return (
            <option>RoleType</option>        
    )
}