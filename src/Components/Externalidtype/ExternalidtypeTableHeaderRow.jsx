
import { ExternalidtypeTableDefinition as TableDefinition} from './ExternalidtypeTableDefinition';

export const ExternalidtypeTableHeaderRow = ({ tabledefinition=TableDefinition, children }) => {
    return (
        <tr>
            {Object.entries(tabledefinition).map(
                ([name, value]) => <th key={ name }>{ value.label }</th>
            )}
            {children}
        </tr>
    )
}

