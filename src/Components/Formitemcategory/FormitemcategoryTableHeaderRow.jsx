
import { FormitemcategoryTableDefinition as TableDefinition} from './FormitemcategoryTableDefinition';

export const FormitemcategoryTableHeaderRow = ({ tabledefinition=TableDefinition, children }) => {
    return (
        <tr>
            {Object.entries(tabledefinition).map(
                ([name, value]) => <th key={ name }>{ value.label }</th>
            )}
            {children}
        </tr>
    )
}

