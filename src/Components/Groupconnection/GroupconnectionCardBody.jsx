import Row from 'react-bootstrap/Row'
import Col from 'react-bootstrap/Col'

export const GroupconnectionCardBody = ({ groupconnection, children }) => {
    return (
        <>
            {children}
        </>
    )
}

