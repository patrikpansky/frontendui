
import { QuestionvalueTableDefinition as TableDefinition} from './QuestionvalueTableDefinition';

export const QuestionvalueTableHeaderRow = ({ tabledefinition=TableDefinition, children }) => {
    return (
        <tr>
            {Object.entries(tabledefinition).map(
                ([name, value]) => <th key={ name }>{ value.label }</th>
            )}
            {children}
        </tr>
    )
}

